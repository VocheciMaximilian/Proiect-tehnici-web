<header>
    <h1>Găteală</h1>
    <nav>
        <div class="menu-container">
            <!-- Butonul Hamburger pentru ecrane mici -->
            <div class="menu-trigger">
                <span></span>
                <span></span>
                <span></span>
            </div>
            <ul class="menu">
                <li>
                    <a href="/">
                        <i class="fa fa-home"></i> <span>Acasă</span>
                    </a>
                </li>
                <!-- Submeniu: Rețete -->
                <li class="has-submenu">
                    <a href="/retete">Rețete</a>
                    <ul class="submenu">
                        <li><a href="/retete?categorie=Aperitive">Aperitive</a></li>
                        <li><a href="/retete?categorie=Feluri principale">Feluri Principale</a></li>
                        <li><a href="/retete?categorie=Deserturi">Deserturi</a></li>
                    </ul>
                </li>
                <li><a href="#populare">Rețete Populare</a></li>
                <li><a href="#rapide">Rețete Rapide</a></li>
                <li><a href="#autentificare">Autentificare / Înregistrare</a></li>
                <!-- Submeniu: Informații -->
                <li class="has-submenu">
                    <a href="#">Informații</a>
                    <ul class="submenu">
                        <li><a href="#contact">Contact</a></li>
                        <li><a href="#despre">Despre noi</a></li>
                    </ul>
                </li>
                <li><a href="#contact">Contact</a></li>
                <li><a href="/video-vtt">Video-VTT</a></li>
                <!-- Theme Toggle Button -->
                <li class="theme-toggle">
                    <button id="theme-toggle-btn" class="btn-custom">
                        <i class="fa fa-moon-o"></i> <span>Dark Mode</span>
                    </button>
                </li>
            </ul>
        </div>
    </nav>
</header>

<script>
// Theme toggle functionality
document.addEventListener('DOMContentLoaded', function() {
    const themeToggleBtn = document.getElementById('theme-toggle-btn');
    const body = document.body;
    
    // Get saved theme from localStorage
    const savedTheme = localStorage.getItem('theme') || 'light';
    
    // Apply saved theme
    if (savedTheme === 'dark') {
        body.classList.add('theme-dark');
        body.classList.remove('theme-light');
        updateButtonText(true);
    } else {
        body.classList.add('theme-light');
        body.classList.remove('theme-dark');
        updateButtonText(false);
    }
    
    // Toggle theme on button click
    themeToggleBtn.addEventListener('click', function() {
        if (body.classList.contains('theme-dark')) {
            // Switch to light theme
            body.classList.remove('theme-dark');
            body.classList.add('theme-light');
            localStorage.setItem('theme', 'light');
            updateButtonText(false);
        } else {
            // Switch to dark theme
            body.classList.remove('theme-light');
            body.classList.add('theme-dark');
            localStorage.setItem('theme', 'dark');
            updateButtonText(true);
        }
    });
    
    function updateButtonText(isDark) {
        const icon = themeToggleBtn.querySelector('i');
        const text = themeToggleBtn.querySelector('span');
        
        if (isDark) {
            icon.className = 'fa fa-sun-o';
            text.textContent = 'Light Mode';
        } else {
            icon.className = 'fa fa-moon-o';
            text.textContent = 'Dark Mode';
        }
    }
});
</script>
